# Data Folder

- `synthetic_fb_ads_undergarments.csv`
  Full synthetic dataset from the assignment. Place it here.

- `sample_fb_ads.csv`
  A smaller subset (e.g., 500–2000 rows) used for fast development.

Config switch in `config/config.yaml`:

```yaml
use_sample_data: true   # use sample data
# use_sample_data: false  # use full dataset
